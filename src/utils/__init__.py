"""
Utility modules for the Flask Binary Repository Management System.

This package contains shared utility functions used across the application:

- validation_utils — Input validation, name sanitization, path normalization,
  and size limit enforcement
- format_utils — Format detection, content type mapping, and coordinate parsing
- crypto_utils — Hash computation, checksum validation, and encryption utilities
"""
